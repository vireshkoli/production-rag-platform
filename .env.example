# Copy to .env and fill in. Never commit .env.

# Required: powers answer generation, the groundedness verifier, and the eval judge.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides (defaults shown)
# RAG_GENERATION_MODEL=claude-haiku-4-5
# RAG_JUDGE_MODEL=claude-haiku-4-5
# RAG_QDRANT_PATH=data/qdrant
# RAG_TELEMETRY_DB=data/telemetry.sqlite3
# RAG_GROUNDEDNESS_THRESHOLD=0.75
# RAG_MAX_QUERY_COST_USD=0.05
